# 教務webアプリ

## アプリの概要

生徒の出欠や成績等のデータを管理し、調査書、指導要録の印刷までを行う。

## アプリの設計
- ホームディレクトリで`npm run docs`して生成される`out/index.html`を参照する。
これはこのページ及び、ソースコードに記したコメントからjsdocで生成されるドキュメントである。
- (自分へのメッセージ)ソースコードを追加する際には、必ずjsdocの形式のコメントを付けること。
- (自分へのメッセージ)このページに可能な限り設計を書き残すこと。

### 全体構成
- SPA(Single Page webApplication)である。サーバ側では認証、データの保持のみを行い、その他のほとんどの処理はクライアント側で行う。
- 開発に割けるパワーが少なく、メンテナンスし易くするために、可能な限り依存関係を減らしてシンプルな構成を目指している。

```mermaid
graph TD;
    A-->B;
    A-->C;
    B-->D;
    C-->D;
```

### サーバ側
後で書く

### クライアント側
- ブラウザで該当URLにアクセスする

## アプリの導入
### 導入手順(全体)
準備が必要なのはサーバ側である。以下を行う。
- このリポジトリをcloneする
- `npm install`
- `lib/keys.js`にあるhttpsの鍵を変更
- mongodbにユーザ等のデータを入れる
- `node app.js`

### 導入手順(データの準備)
後で書く

### バージョン
- node.js : v20
- mongodb : v8
詳しくはpackage.jsonを見よ。
